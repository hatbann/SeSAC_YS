<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/main.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <title>Main</title>
</head>
<body>
    <section>
        <div id="mainBtns">
                <button type="button" onclick="goLoginPage()">로그인</button>
                <button type="button">회원가입</button>
        </div>
        <div id="welcomediv" class="displayX"> 
            <h1 id="welcome"><span><%=id%></span>님 환영합니다</h1>
            <button id="logout"  onclick="logoutPage()">로그아웃</button>
        </div>
    </section>
    <script>
        let login = "<%=islogin%>";
let mainBtns =  document.querySelector('#mainBtns');
let logout = document.querySelector('#logout');
let welcomediv  = document.querySelector('#welcomediv');
if(login === "true"){
    mainBtns.classList.add('displayX');
    welcomediv.classList.remove('displayX');
}else{
    mainBtns.classList.remove('displayX');
    welcomediv.classList.add('displayX');
}

function goLoginPage(){
    location.href = '/login';
    /*axios(
        {
            method: 'get',
            url: "/login",
        }
    )*/
}

function logoutPage(){
    axios({
        method: "post",
        url: "/logout",
        data:{
        }
    }).then(()=>{
        location.href = '/';
    })
}
    </script>
</body>
</html>