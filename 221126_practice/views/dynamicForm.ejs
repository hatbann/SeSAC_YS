<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
        /*
        function onDynamicUpload(){
            let form = document.getElementById('dynamicForm');
            let formdata = new FormData(form);
            axios({
                headers: {
                    "Content-Type": "multipart/form-data",
                 },
                method:"post",
                url:"/dynamicForm",
                data: formdata,
            }).then((res)=>{
                return res.data;
            }).then((d)=>{
                const dynamicImg = document.getElementById('dynamicImg').src = d.path;
            })
        }
        */

        function onDynamicUpload(){
            let form = document.getElementById('dynamicForm');
            const file = document.getElementById('fileinput');
            let formdata = new FormData();
            console.log(file.files[0]);
            formdata.append("dynamicfile", file.files[0]); //single() 안의 파라미터 값이 append의 첫번쩨ㅐ 매개변수로 이름같게!
            axios({
                headers: {
                    "Content-Type": "multipart/form-data",
                 },
                method:"post",
                url:"/dynamicForm",
                data: formdata,
            }).then((res)=>{
                return res.data;
            }).then((d)=>{
                const dynamicImg = document.getElementById('dynamicImg').src = d.path;
            })
        }
    </script>
    <title>Document</title>
</head>
<body>
    <h3>동적 파일 업로드</h3>
    <form action="dynamic" method="post" enctype="multipart/form-data" id="dynamicForm">
        <input type="file" name="dynamicfile" id="fileinput" >
        <button type="button" onclick="onDynamicUpload()">업로드</button>
    </form>
    <img  id="dynamicImg">
</body>
</html>